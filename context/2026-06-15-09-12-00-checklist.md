---
date: 2026-06-15
session: round 28 — bug 409 conflict no register de user soft-deletado
---

# Checklist — Round 28

## FEITO

### Diagnóstico
- ✓ Investigado /var/log/caddy-waf/audit.log: 3x 409 em /v1/auth/user/register
      do IP do user (194.15.111.214, Firefox 151) em 08:41:27/42/57
- ✓ Confirmado no Postgres: email `k3ntluk@gmail.com` soft-deletado em
      2026-06-12 14:45 (durante cleanup de QA)
- ✓ Root cause identificada: GetByEmail não filtrava deleted_at IS NULL +
      UNIQUE constraint total bloqueando INSERT mesmo após fix do GetByEmail

### Implementação
- ✓ `viralefy_core/migrations/047_users_email_unique_partial.up.sql`:
      DROP CONSTRAINT users_email_key + CREATE UNIQUE INDEX parcial
      WHERE deleted_at IS NULL
- ✓ `viralefy_core/migrations/047_users_email_unique_partial.down.sql`:
      rollback que falha gracefully se há duplicatas
- ✓ `viralefy_auth/user_repo.go` GetByEmail: filtra `AND deleted_at IS NULL`
- ✓ `viralefy_core/user_repo.go` GetByEmail: mesma mudança

### Testes
- ✓ go build viralefy_auth — OK
- ✓ go build viralefy_core — OK
- ✓ go test ./... viralefy_auth — todos PASS (application 0.643s)
- ✓ go test ./... viralefy_core — todos PASS (application 0.395s)

### Deploy
- ✓ git commit + push em viralefy_auth (e59d951)
- ✓ git commit + push em viralefy_core (2d91b18)
- ✓ viralefy-update em prod — zero-downtime, todos services restart OK
- ✓ Migration 047 aplicada (gomigrate via update script)
- ✓ Smoke 9/9 PASS
- ✓ Critical-flows 20/20 PASS após deploy
- ✓ Validação manual em prod: POST register k3ntluk@gmail.com → 201

### Monitoramento (anti-regressão)
- ✓ `viralefy-critical-flows` ganhou `flow_db_invariant_softdelete_reuse`:
      verifica índice parcial presente + UNIQUE total ausente. Roda só se
      psql disponível (skipa em local sem barulho).
- ✓ Deploy do novo script em /usr/local/sbin/viralefy-critical-flows
- ✓ Run completo em prod: 22/22 PASS
- ✓ git commit + push viralefy_ops (63518d3)

### Archive
- ✓ context.md gerado
- ✓ checklist.md gerado (este)

## EM ABERTO / A FAZER

### Curto prazo (próximo round, se opt-in)
- `viralefy_api` legacy: verificar se tem GetByEmail equivalente e aplicar fix
  (próximo passo concreto: `grep -rn "GetByEmail" viralefy_api/internal/`).
  Critério de pronto: se existir, mesma mudança; se não, documentar como
  N/A.
- Test unitário com Postgres real (testcontainers) cobrindo GetByEmail filtro
  + register reuso. Tests atuais são in-memory e não cobrem o filtro SQL.
  Próximo passo: usar `testcontainers-go/postgres` no
  `viralefy_core/internal/infrastructure/persistence/postgres/user_repo_test.go`.

### Médio prazo
- Considerar fluxo de UI: se user tenta register com email que JÁ existe ativo,
  o frontend mostra erro descente "email já cadastrado, tente recuperar senha"?
  Investigação: olhar componente `RegisterForm` em viralefy_front.

## NÃO REFAZER

- NÃO restaurar `users_email_key` (UNIQUE total): bloqueia reuso após soft-delete
  e quebra LGPD Art. 18 IV.
- NÃO adicionar autoreviver de user soft-deletado em RegisterUser: cria
  ambiguidade sobre conta histórica vs nova. Cada register = registro novo.
- NÃO remover GetByEmail filtro pensando que login precisa "ver" deleted:
  login já fazia check `DeletedAt == nil` defensivo; filtro no SQL é mais
  simples e correto.
- NÃO mexer no anti-enum (RequestPasswordReset): retorna success-but-no-op
  pra emails não encontrados — essencial pra não vazar existência de conta.

## Tempo total
~50 min do report do user ao 22/22 PASS final em prod, archive e commit.

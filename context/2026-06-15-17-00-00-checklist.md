---
date: 2026-06-15
session: round 29 — UX 409 + mensagem útil + ApiError tipado
---

# Checklist — Round 29

## FEITO

### Backend
- ✓ `viralefy_auth/internal/application/auth_service.go` RegisterUser:
      wrap `fmt.Errorf("email already registered: %w", domain.ErrConflict)`
- ✓ `viralefy_core/internal/application/user_auth_service.go` Register:
      mesmo wrap
- ✓ `viralefy_auth/internal/interface/http/response.go`: strings.TrimSuffix
      no case ErrConflict — mensagem fica limpa "email already registered"
- ✓ `viralefy_core/internal/interface/http/response.go`: mesma mudança
- ✓ go build em ambos — OK
- ✓ go test ./... em ambos — todos PASS

### Frontend
- ✓ `viralefy_front/src/lib/api.ts`: nova class `ApiError(status, code, message)`
- ✓ `request<T>` agora joga ApiError em vez de Error genérico
- ✓ `app/register/page.tsx`: `error` typed como `ReactNode | null`,
      branch CONFLICT mostra CTA "Sign in" + "recover password" com links
- ✓ `app/login/page.tsx`: branch UNAUTHORIZED com mensagem clara,
      RATE_LIMITED tratado
- ✓ npm test — 501/501 PASS
- ✓ Mantém anti-enum (login não distingue email vs senha errada)

### Deploy
- ✓ git commit + push viralefy_auth (6004aa3 + d8b504e)
- ✓ git commit + push viralefy_core (5aafd2c + 0360257)
- ✓ git commit + push viralefy_front (058d19d)
- ✓ viralefy-update — zero-downtime, 7 services restart OK
- ✓ smoke 9/9 PASS
- ✓ critical-flows 22/22 PASS após deploy
- ✓ Validação manual em prod: POST register com email existente →
      `{"error":{"code":"CONFLICT","message":"email already registered",...}}`

### viralefy_api legacy
- ✓ Verificado: service INACTIVE/DEAD; dispatcher Rust substituiu.
      Política do CLAUDE.md proíbe mudanças sem incidente em prod legacy.
      Documentado como "skip por design".

### Archive
- ✓ context.md
- ✓ checklist.md (este)

## EM ABERTO / A FAZER

### Curto prazo
- Adicionar test cobrindo a mensagem do 409 no response.go (regression):
  arquivo `viralefy_auth/internal/interface/http/response_test.go` ou
  `viralefy_core/internal/interface/http/response_test.go`. Critério de
  pronto: input `fmt.Errorf("email already registered: %w", ErrConflict)`
  → body com message exata.
- Test integration cobrindo o flow front: simular ApiError com code=CONFLICT
  e asserter que o JSX renderiza os links "Sign in" e "recover password".
  Arquivo: `viralefy_front/tests/unit/register-error-ux.test.mjs` (novo).

### Médio prazo
- i18n: hoje todas as strings ("email already registered", "Incorrect
  email or password", "Too many attempts…") estão em EN no front.
  Quando a app for traduzida (já tem infra de 26 idiomas), criar entries
  pra essas strings em `src/i18n/` (ou similar).
- Repassar outros endpoints que retornam CONFLICT no backoffice (admin
  register, gateway delete) e padronizar mensagens da mesma maneira.

## NÃO REFAZER

- NÃO remover o anti-enum no login: a mensagem unificada "Incorrect
  email or password" cobre tanto email-não-existe quanto senha-errada.
  Vazar isso é OWASP A07.
- NÃO usar Error genérico no front: a class ApiError é essencial pra
  UX granular. Sem `code` + `status`, o frontend volta a mostrar alertas
  opacos como "conflict" ou "unauthorized".
- NÃO mexer no viralefy_api legacy: CLAUDE.md proíbe.

## Tempo total
~45 min do "continue" do round 28 ao 22/22 final + archive commit.

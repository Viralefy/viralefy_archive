---
date: 2026-06-16
session: round 30 — regression tests do 409/ApiError
---

# Checklist — Round 30

## FEITO

### Backend tests
- ✓ viralefy_auth: `internal/interface/http/response_test.go` criado com
      4 tests cobrindo bare/wrapped/deeply-wrapped/anti-enum. PASS 0.004s.
- ✓ viralefy_core: response_test.go estendido com 3 tests novos
      (BareSentinel, WrappedWithUsefulMessage, DeeplyWrapped). PASS.
- ✓ go test ./... em ambos — todos verdes, zero regressão.

### Frontend tests
- ✓ `tests/unit/api-error.test.mjs` criado com 6 tests cobrindo
      ApiError class shape + tradução de 409/401/429 + fallback REQUEST_FAILED.
- ✓ `tests/ts-loader-hooks.mjs` estendido com resolução de imports
      relativos sem extensão. Necessário pra importar `lib/api.ts` que
      faz value import de `./schemas`. Não muda comportamento de aliases.
- ✓ 507/507 PASS na suite full (era 501; +6 deste round).

### Commits + push
- ✓ viralefy_auth@5123660 pushed
- ✓ viralefy_core@c6ec8bb pushed
- ✓ viralefy_front@25f5020 pushed

### Archive
- ✓ context.md
- ✓ checklist.md (este)

## EM ABERTO / A FAZER

### Curto prazo
- Postgres integration test pra GetByEmail filtro (testcontainers-go
  ou pgxmock). Reaberto do round 28. Próximo passo concreto: adicionar
  `viralefy_core/internal/infrastructure/persistence/postgres/user_repo_integration_test.go`
  com build tag `integration` + testcontainers. Critério: 3 testes —
  user ativo retorna registro, soft-deleted retorna ErrNotFound,
  email não existe retorna ErrNotFound.

### Médio prazo
- Backoffice (admin register, gateway delete): repassar endpoints que
  retornam CONFLICT e padronizar shape da mensagem. Próximo passo:
  `grep -rn "ErrConflict\|StatusConflict" viralefy_core/internal/interface/http/`
  e identificar quais já têm mensagem útil.
- i18n das strings de erro do front: hoje EN-only no register/login.
  Quando a app passar a usar i18n nas mensagens, adicionar entries em
  `src/i18n/messages/` (ou onde estiver).

## NÃO REFAZER

- NÃO remover `strings.TrimSuffix` do response.go: tests trancam o
  contrato. Sem ele a mensagem volta a vazar ": conflict".
- NÃO downgrade ApiError pra Error genérico: tests reprovam.
- NÃO ampliar o loader pra resolver imports não-`@/` que não comecem
  com `./` ou `../`: vai pegar specifiers de node_modules incorretamente.

## Tempo total
~30 min do "continue" ao 507/507 final + archive commit.
